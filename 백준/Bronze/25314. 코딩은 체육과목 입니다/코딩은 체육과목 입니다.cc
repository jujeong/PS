#include <bits/stdc++.h>
using namespace std;

int N;

void input() {
    cin >> N;
}

void output() {
    for (int i = 0; i < N / 4; i++) {
        cout << "long ";
    }
    cout << "int";
}

void progress() {
    
}

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);

    input();
    progress();
    output();
}